#!/bin/sh
# Make all script files executable

chmod +x build-all.sh
chmod +x build-no-lint.sh
chmod +x build-api-no-check.sh
chmod +x clean-build.sh
chmod +x ensure-files.sh
chmod +x ensure-policies.sh

echo "All script files are now executable."
